Lab 2 - Array Algorithms

Student: Cyril - 22307204

Files
 `FindMax.java` — Finds the largest value in an array and its index (Brute Force, O(n))
 `PairSum.java` — Finds a unique pair of numbers summing to a target value (Brute Force Nested Loop, O(n²))

## How to Run
```
javac FindMax.java && java FindMax
javac PairSum.java && java PairSum
```
